import React from 'react'
import { Link } from 'react-router-dom'
import { track } from '../lib/analytics'
export default function Home(){React.useEffect(()=>track('view_home'),[]);return(<div className='grid' style={{maxWidth:840,margin:'0 auto'}}><div className='card'><h1 style={{marginTop:0}}>Welcome to the Island</h1><p>Three experiments in contrastive learning — felt, not explained.</p><ul><li><strong>Predict the Future Frame</strong> — choose what comes five steps later.</li><li><strong>Label the Feeling</strong> — cluster by vibe without labels.</li><li><strong>Contrastive Goat</strong> — pick the next goat in the sequence.</li></ul><div style={{display:'flex',gap:'.5rem',marginTop:'.5rem'}}><Link className='btn' to='/predict' onClick={()=>track('nav_predict')}>Begin: Predict</Link><Link className='btn' to='/feelings' onClick={()=>track('nav_feelings')}>Begin: Feelings</Link><Link className='btn' to='/goat' onClick={()=>track('nav_goat')}>Begin: Goat</Link></div></div><div className='card'><p style={{opacity:.8}}><em>“You learned to predict — not because you remembered, but because you contrasted.”</em></p></div></div>)}
